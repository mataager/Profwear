// Set the background images using the URLs

document.getElementById(
  "new-arrival-img"
).style.backgroundImage = `url('${newarrivalurl}')`;

document.getElementById("sale-img").style.backgroundImage = `url('${saleurl}')`;
